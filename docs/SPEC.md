🎯 1. Цель проекта
Создать минимально жизнеспособный продукт (MVP) для Android, позволяющий пользователю хранить цифровые копии пластиковых карт лояльности (штрихкоды, QR-коды, номера) и быстро предоставлять их на кассе.

👤 2. Целевой пользователь

Для начала - сами разработчики
В будущем: любой человек, посещающий супермаркеты, кофейни, аптеки и имеющий overdohua пластиковых карт.

📊 3. Основные функции 

Добавление карты:

- Ручной ввод: название магазина, номер карты, штрихкод (ввод вручную или сканером камеры).
- Фотографирование карты с возможностью обрезки (в первом релизе не обязательно).
- Автоматическое распознавание номера и названия с фото (можно использовать ML Kit от Google) (для первого релиза пропускаем).

Хранение и просмотр:

Главный экран — список/сетка добавленных карт (иконка + название). Подумать, где разместить группы карт.
Экран деталей карты: крупное изображение штрихкода/QR/номера, которое можно показать кассиру.

Быстрый доступ:

Поиск по названию магазина.
Доступ из папок в группе. При выходе из приложения запоминать последнее место (эндпоинт навигации)
Не обязательно: Виджет на рабочий стол для быстрого показа штрихкода самой частой карты.

***Управление отображением и навигация:***

*Экран-заставка (Splash Screen):* Отображается при запуске приложения в течение 1 секунды. Содержит логотип команды разработчиков. Не имеет навигации.

*Главный экран:* Содержит коллекцию карточек и нижнюю панель навигации (Bottom Navigation Bar) с разделами:
- Избранные (показывает карточки, отмеченные пользователем как избранные)
- Все карты (основной режим просмотра всей коллекции)
- Настройки

*Гибкий режим отображения коллекции:* В разделе "Все карты" пользователь может выбрать в настройках один из трёх вариантов:

1) Список (одна колонка, RecyclerView в LinearLayoutManager).
2) Сетка (2 колонки) (GridLayoutManager).
3) Авто (адаптивная сетка: 1 колонка при ≤3 карточках, 2 колонки при 4-8 карточках, 3 колонки при ≥9 карточках).

*Кнопка добавления:* В режиме просмотра коллекции кнопка "+" для добавления новой карты интегрирована как последний элемент списка/сетки.

⚙️ 4. Технический стек (рекомендация для джуна)

Язык: Kotlin (стандарт для современной Android-разработки).
Архитектура: MVVM
Локальная база данных: Room Persistence Library (SQLite)
Работа с камерой/изображениями: CameraX.
Генерация штрихкодов: Библиотека типа ZXing или BarcodeMask.

*Минимальная версия Android: API 26 (Android 7.0)*

🎨 5. Дизайн и UX (минимализм)

Главный принцип: Карта должна быть показана в 1-2 тапа от разблокированного телефона.
Экран деталей карты: Минимум элементов, максимальный контраст штрихкода на белом фоне (чтобы сканер кассы сработал).
Минимум 3 темы: Светлая, тёмная, кастомная (подумать).

🗺 6. Дорожная карта разработки (этапы)

**Спринт 1: Каркас**

Настройка проекта в Android Studio.
Пустой экран с BottomNavigationView.
Подключение необходимых библиотек.
Создание базы данных и сущности LoyaltyCard.

**Спринт 2: Добавление и хранение**

Экран/диалог для добавления карты: поля ввода, кнопка "Сфотографировать".
Работа с камерой (CameraX), сохранение фото в Bitmap.
Сохранение карты в Room.
Простой список на главном экране (RecyclerView).

**Спринт 3: Отображение и виджет**

Экран деталей карты с полноразмерным штрихкодом.
Генерация штрихкода из номера карты (библиотека).
Ключевая задача: Создание простого виджета (App Widget), который показывает штрихкод выбранной карты (с возможностью прокрутки прямо в виджете).

**Спринт 4: Полировка и отладка**

Поиск по картам.
Удаление/редактирование карт.
Тестирование на разных устройствах/версиях Android.
Проверка, что штрихкод с экрана считывается реальными сканерами.

🚀 7. Дальнейшие шаги 

Сортировка карт (по частоте использования, алфавиту).
Категории карт (еда, аптека, развлечения).
История использования (когда показывали карту).
Анимации.

8. Замечания к разработке

Пояснения и ограничения:

Splash Screen: Должен быть реализован через Theme (согласно современным гайдлайнам Android), а не отдельной активностью с Thread.sleep(). Это важно для быстрого запуска.
Кнопка добавления в списке: Нужно будет использовать RecyclerView с разными типами ViewHolder (например, TYPE_CARD и TYPE_ADD_BUTTON). Это ключевая сложность.
Навигация: Состояние выбранного раздела в BottomNavigationView должно сохраняться при повороте экрана.
Режим "Авто": Логику расчёта колонок стоит вынести в отдельный класс LayoutManagerHelper. Она должна пересчитываться при каждом добавлении/удалении карты.
Производительность: При большом количестве карт (50+) и сложной сетке нужно обеспечить плавную прокрутку. Это повлияет на выбор способа загрузки изображений (например, Glide/Picasso).
